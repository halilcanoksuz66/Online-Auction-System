import 'package:flutter/material.dart'; // Buraya widgets.dart yazmadık

EdgeInsets paddingAll(double index) => EdgeInsets.all(index);
EdgeInsets vertical(double index) => EdgeInsets.symmetric(vertical: index);
EdgeInsets horizontal(double index) => EdgeInsets.symmetric(horizontal: index);
